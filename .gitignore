node_modules/
*.log
.DS_Store

# Packed tarballs produced by `pnpm pack` / the consumer smoke test.
*.tgz

# Editor
.idea/
*.iml

# Build / coverage (this is a types-only package; .d.ts are the published sources)
dist/
coverage/

# Local-only: the raw Javadoc HTML pulled from a Mirth container.
# Tracked specs make generation reproducible; flip this if the dumps are large.
# javadoc/
